Step to create SAP HANA Projet - Microservice to create table, view and role

1. Create new SAP HANA Project from template
2. Create table
    Create new folder called table inside src and add file (check table folder)
3. Create view
    Create ew folder called views inside src an add file (check view folder)
4. Create role
    Create new folder calle roles inside src and add file (check role folder)
5. Note: Schema is bee added in the mta.yaml, maintain some relevent name. This will e schema name for the table and view
    schema: BTP_DB_HC